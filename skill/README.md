# Skill Wrapper Source

This directory is the local source anchor for the packaged skill. The installer
chooses the destination skill directory from `--skills-dir`,
`BURPVALVE_SKILLS_DIR`, or the default `~/skills`.
